<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2PC算法和共识算法</title>
    <url>/2023/05/30/2PC%E7%AE%97%E6%B3%95%E5%92%8C%E5%85%B1%E8%AF%86%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>TODO</p>
]]></content>
      <categories>
        <category>基础</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>Elasticsearch的选举机制</title>
    <url>/2023/05/30/Elasticsearch%E7%9A%84%E9%80%89%E4%B8%BE%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>TODO</p>
]]></content>
      <categories>
        <category>es</category>
      </categories>
  </entry>
  <entry>
    <title>Log4j2使用</title>
    <url>/2024/05/21/Log4j2%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h3 id="log4j2的基础使用方式"><a href="#log4j2的基础使用方式" class="headerlink" title="log4j2的基础使用方式"></a>log4j2的基础使用方式</h3><p>log4j2是apache的一个使用非常广泛的日志框架，用于记录应用程序的日志信息。</p>
<h4 id="需要的依赖"><a href="#需要的依赖" class="headerlink" title="需要的依赖"></a>需要的依赖</h4><p>要想使用该日志框架，需要引入如下依赖。</p>
<span id="more"></span>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- &lt;log4j2.version&gt;2.17.2&lt;log4j2.version&gt; --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.logging.log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;log4j2.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.logging.log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;log4j2.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="需要的配置"><a href="#需要的配置" class="headerlink" title="需要的配置"></a>需要的配置</h4><p>同时在resources目录下，创建日志配置文件，指定将日志输出到控制台。</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment"># log4j2.properties</span></span><br><span class="line"><span class="comment"># 定义log4j2自身内部日志的级别</span></span><br><span class="line"><span class="attr">status</span> = <span class="string">INFO  </span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"># appender定义</span></span><br><span class="line"><span class="attr">appender.console.type</span> = <span class="string">Console</span></span><br><span class="line"><span class="attr">appender.console.name</span> = <span class="string">STDOUT</span></span><br><span class="line"><span class="attr">appender.console.layout.type</span> = <span class="string">PatternLayout </span></span><br><span class="line"><span class="attr">appender.console.layout.pattern</span> = <span class="string">%d&#123;HH:mm:ss.SSS&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n </span></span><br><span class="line"><span class="comment">  </span></span><br><span class="line"><span class="comment"># 日志记录器定义  </span></span><br><span class="line"><span class="attr">rootLogger.level</span> = <span class="string">debug  </span></span><br><span class="line"><span class="attr">rootLogger.appenderRefs</span> = <span class="string">stdout  </span></span><br><span class="line"><span class="attr">rootLogger.appenderRef.stdout.ref</span> = <span class="string">STDOUT</span></span><br></pre></td></tr></table></figure>

<p>就上面的日志配置，解释如下。</p>
<h5 id="log4j2自身内部日志级别"><a href="#log4j2自身内部日志级别" class="headerlink" title="log4j2自身内部日志级别"></a>log4j2自身内部日志级别</h5><p>log4j2作为一个日志框架，其在启动，初始化配置，或者其他事件的时候，也会产生一些日志，这些日志被称之为log4j2自身内部的日志。<br>我们通过<code>status = INFO</code>，实际上是定义了这些日志的级别。</p>
<h5 id="日志记录器和日志输出器"><a href="#日志记录器和日志输出器" class="headerlink" title="日志记录器和日志输出器"></a>日志记录器和日志输出器</h5><p>在log4j2中，有两个核心概念。</p>
<ul>
<li>日志记录器（<code>rootLogger/logger</code>）：用于在代码中输出日志信息。<code>rootLogger</code>是全局的根记录器，负责管理所有日志的默认行为。</li>
<li>日志输出器（<code>appender</code>）：负责将日志输出到指定的位置，比如控制台或文件。如上所示，我们定义了一个<code>appender</code>，该<code>appender</code>的功能就是可以将日志输出到控制台。</li>
</ul>
<h5 id="appender配置的逐行解释"><a href="#appender配置的逐行解释" class="headerlink" title="appender配置的逐行解释"></a>appender配置的逐行解释</h5><ul>
<li><code>appender.console.type = Console</code>：定义了一个名为<code>console</code>的日志输出器，其类型为<code>Console</code>，表示将日志输出到控制台。</li>
<li><code>appender.console.name = STDOUT</code>：定义了一个名字叫<code>STDOUT</code>的输出器，这个是该appender的唯一标识符，logger可以通过该name引用这个appender。</li>
<li><code>appender.console.layout.type = PatternLayout</code>：定义了appender的布局类型为<code>PatternLayout</code>，表示日志内容的格式化方式，<code>PatternLayout</code>允许你通过一个模式字符串来完全自定义日志消息的输出格式。其他布局类型还包括<code>JSONLayout</code>、<code>XMLLayout</code>等。</li>
<li><code>appender.console.layout.pattern = %d&#123;HH:mm:ss.SSS&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n</code>：定义了日志输出的格式模式。</li>
</ul>
<p>其中<code>appender.console.layout.pattern</code>的格式说明如下：</p>
<ul>
<li><code>%d&#123;HH:mm:ss.SSS&#125;</code>：表示日志事件的时间戳，格式为小时:分钟:秒.毫秒。</li>
<li><code>[%t]</code>：表示当前线程的名称。</li>
<li><code>%-5level</code>：表示日志级别，左对齐，占5个字符宽度。</li>
<li><code>%logger&#123;36&#125;</code>：表示日志记录器的名称，最多显示36个字符。</li>
<li><code>-</code>：表示日志消息前的分隔符，只是为了格式美观</li>
<li><code>%msg</code>：表示日志消息内容</li>
<li><code>%n</code>：表示换行符, 确保每条日志消息后都有一个换行。</li>
</ul>
<h5 id="logger配置的逐行解释"><a href="#logger配置的逐行解释" class="headerlink" title="logger配置的逐行解释"></a>logger配置的逐行解释</h5><ul>
<li><code>rootLogger.level = debug</code>：定义了根记录器的日志级别为<code>debug</code>。</li>
<li><code>rootLogger.appenderRefs = stdout</code>：指定<code>rootLogger</code>将引用哪些Appender来输出日志，appenderRefs是一个列表，可以包含多个appender, 表示logger可以将这个日志时间发送给多个appender进行输出。这里仅只定了一个，表示<code>rootLogger</code>将会把日志时间发送给名为<code>stdout</code>的appender。</li>
<li><code>rootLogger.appenderRef.stdout.ref = STDOUT</code>：这里首先定义一个名字叫<code>stdout</code>的appender引用，该引用实际指向的appender指向上面定义的<code>STDOUT</code>，表示将日志输出到控制台。</li>
</ul>
<h4 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h4><p>在Java代码中，使用该日志框架。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.apache.logging.log4j.LogManager;  </span><br><span class="line"><span class="keyword">import</span> org.apache.logging.log4j.Logger;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">App</span> &#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Logger</span> <span class="variable">log</span> <span class="operator">=</span> LogManager.getLogger(App.class);  </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;  </span><br><span class="line">        log.info(<span class="string">&quot;hello world&quot;</span>);  </span><br><span class="line">        log.debug(<span class="string">&quot;--------------&quot;</span>);  </span><br><span class="line">        System.out.println(<span class="string">&quot;aaaa&quot;</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行，在控制台输出如下内容。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">17:18:59.080 [main] INFO  com.guluo.App - hello world</span><br><span class="line">17:18:59.087 [main] DEBUG com.guluo.App - --------------</span><br><span class="line">aaaa</span><br></pre></td></tr></table></figure>

<h3 id="log4j2的实际应用方式"><a href="#log4j2的实际应用方式" class="headerlink" title="log4j2的实际应用方式"></a>log4j2的实际应用方式</h3><p>在实际开发中，我们通常不会直接在 Java 代码中使用 log4j2 的 API，因为这样会导致业务代码与具体日志框架强耦合，不利于后续日志框架的替换和维护。</p>
<p>为了实现业务代码与实际的日志框架解耦，常用的办法就是引入slf4j。</p>
<p>slf4j 主要为 Java 日志访问提供了一个标准化的 API 框架，其核心作用是作为日志门面，定义统一的接口，具体的日志实现则由其他框架（如 log4j、logback 等）完成。虽然 slf4j 自身也提供了简单的实现，但实际项目中很少直接使用。通常，Java 项目会选择 slf4j-api 作为日志门面，结合具体实现框架（如 log4j2、logback），并通过桥接器进行集成。</p>
<p>例如，项目中以 slf4j 作为统一日志入口，实际日志实现采用 log4j2 时，可以使用 <code>log4j-slf4j-impl</code> 或 <code>slf4j-log4j12</code> 进行绑定。两者区别在于：</p>
<ul>
<li><code>slf4j-log4j12</code> 主要用于对接 log4j 1.x 版本（虽然也可用于 log4j2，但不推荐）；</li>
<li><code>log4j-slf4j-impl</code> 专为 log4j2 设计，推荐用于 log4j2 集成。</li>
</ul>
<p>这样可以实现业务代码与日志实现的解耦，便于后续维护和替换日志框架。</p>
<h4 id="log4j2与log4j的简单对比"><a href="#log4j2与log4j的简单对比" class="headerlink" title="log4j2与log4j的简单对比"></a>log4j2与log4j的简单对比</h4><p>可以简单认为log4j2是log4j的进阶版本，弥补了log4j日志框架的一些不足。下面是两者的比较。</p>
<ul>
<li>安全性：最显著的区别是安全性。log4j存在一些安全漏洞，其中最为严重的是远程代码执行漏洞，可能导致系统被攻击。log4j2在安全性方面做了改进，修复了这些漏洞，提高了系统的安全性。<br>  详情参考： <a href="https://kingx.me/Patch-log4j.html">Log4j 严重漏洞修复方案参考 CVE-2021-44228 – KINGX</a></li>
<li>性能：log4j2在设计上进行了优化，采用异步日志记录机制，因此在性能方面通常比log4j更高效。log4j2能够更好地处理大量日志记录并提供更好的性能表现。</li>
<li>配置灵活性：log4j2提供了更灵活的配置选项和功能，使得开发人员可以更好地控制日志记录的行为。相对而言，log4j2在配置方面更加灵活和强大。</li>
</ul>
<p>总的来说，log4j2相对于log4j在安全性、性能和配置灵活性方面都有所改进和优势，因此在选择日志框架时，更推荐使用log4j2以获得更好的体验和保障。</p>
<h4 id="集成方式"><a href="#集成方式" class="headerlink" title="集成方式"></a>集成方式</h4><p>由于目前log4j2使用更为普遍，因此在采用<code>log4j-slf4j-impl</code>作为桥接器实现log4j2和slf4j的集成。</p>
<p>在基础使用方式的基础上，再添加依赖，如下所示。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.logging.log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-slf4j-impl<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.17.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后，日志仍然是<code>log4j2.properties</code>，我们额外添加了一个普通<code>logger</code>。</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">status</span> = <span class="string">INFO  </span></span><br><span class="line"><span class="comment">  </span></span><br><span class="line"><span class="comment"># 定义控制台输出  </span></span><br><span class="line"><span class="attr">appender.console.type</span> = <span class="string">Console  </span></span><br><span class="line"><span class="attr">appender.console.name</span> = <span class="string">STDOUT  </span></span><br><span class="line"><span class="attr">appender.console.layout.type</span> = <span class="string">PatternLayout  </span></span><br><span class="line"><span class="attr">appender.console.layout.pattern</span> = <span class="string">%d&#123;HH:mm:ss.SSS&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n  </span></span><br><span class="line"><span class="comment">  </span></span><br><span class="line"><span class="comment"># 根记录器配置  </span></span><br><span class="line"><span class="attr">rootLogger.level</span> = <span class="string">debug  </span></span><br><span class="line"><span class="attr">rootLogger.appenderRefs</span> = <span class="string">stdout  </span></span><br><span class="line"><span class="attr">rootLogger.appenderRef.stdout.ref</span> = <span class="string">STDOUT  </span></span><br><span class="line"><span class="comment">  </span></span><br><span class="line"><span class="comment"># 定义一个普通的logger</span></span><br><span class="line"><span class="attr">logger.http.name</span> = <span class="string">test-name</span></span><br><span class="line"><span class="attr">logger.http.level</span> = <span class="string">info</span></span><br><span class="line"><span class="attr">logger.http.appenderRefs</span> = <span class="string">console</span></span><br><span class="line"><span class="attr">logger.http.appenderRef.console.ref</span> = <span class="string">STDOUT</span></span><br><span class="line"><span class="comment"># 表示不再传递给父 Logger</span></span><br><span class="line"><span class="attr">logger.http.additivity</span> = <span class="string">false</span></span><br></pre></td></tr></table></figure>

<p>Java代码如下所示。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.slf4j.Logger;  </span><br><span class="line"><span class="keyword">import</span> org.slf4j.LoggerFactory;  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SlfLogTest</span> &#123;  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Logger</span> <span class="variable">LOG</span> <span class="operator">=</span> LoggerFactory.getLogger(SlfLogTest.class);  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Logger</span> <span class="variable">test_log</span> <span class="operator">=</span> LoggerFactory.getLogger(<span class="string">&quot;test-name&quot;</span>);  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;  </span><br><span class="line">        LOG.info(<span class="string">&quot;hello world.....&quot;</span>);  </span><br><span class="line">        LOG.debug(<span class="string">&quot;sssssssssssssssssssssss&quot;</span>);  </span><br><span class="line">  </span><br><span class="line">        test_log.info(<span class="string">&quot;AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA&quot;</span>);  </span><br><span class="line">        test_log.debug(<span class="string">&quot;aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa&quot;</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出如下所示。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">11:34:03.573 [main] INFO  com.guluo.SlfLogTest - hello world.....</span><br><span class="line">11:34:03.582 [main] DEBUG com.guluo.SlfLogTest - sssssssssssssssssssssss</span><br><span class="line">11:34:03.583 [main] INFO  test-name - AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA</span><br></pre></td></tr></table></figure>

<p>由于<code>test-name</code>配置的是，仅输出<code>info</code>级别及以上的日志，因此<code>test_log.debug()</code>日志不会输出，结果也与预期一致。</p>
<h4 id="快速切换日志实现"><a href="#快速切换日志实现" class="headerlink" title="快速切换日志实现"></a>快速切换日志实现</h4><p>如果此时因为业务需要，我们需要将程序中的日志实现，从log4j2切换到logback，只需要做如下修改。</p>
<p><strong>更新日志实现的依赖</strong><br>删除上述依赖，并添加如下依赖。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>ch.qos.logback<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>logback-classic<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.2.6<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>slf4j-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.7.30<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>更新日志配置文件</strong><br>删除<code>log4j2.properties</code>，并添加<code>logback.xml</code>，内容如下。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">configuration</span> <span class="attr">scan</span>=<span class="string">&quot;true&quot;</span> <span class="attr">scanPeriod</span>=<span class="string">&quot;60 seconds&quot;</span> <span class="attr">debug</span>=<span class="string">&quot;false&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">&quot;STDOUT&quot;</span> <span class="attr">class</span>=<span class="string">&quot;ch.qos.logback.core.ConsoleAppender&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">encoder</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">pattern</span>&gt;</span>%d&#123;HH:mm:ss.SSS&#125; [%thread] %-5level %logger&#123;36&#125; - %msg%n<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">encoder</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">root</span> <span class="attr">level</span>=<span class="string">&quot;debug&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">&quot;STDOUT&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">logger</span> <span class="attr">name</span>=<span class="string">&quot;test-name&quot;</span> <span class="attr">level</span>=<span class="string">&quot;info&quot;</span> <span class="attr">additivity</span>=<span class="string">&quot;false&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">&quot;STDOUT&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">logger</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>完成上述步骤，即可实现日志框架的快速切换。可以发现，我们仅仅是修改了依赖和配置文件，而业务代码完全不需要修改。<br>这就是使用日志门面（如 slf4j）和具体日志实现（如 log4j2、logback）的好处。通过这种方式，我们可以轻松地在不同的日志框架之间切换，而不需要修改业务代码，从而实现了日志框架的解耦和灵活性。</p>
]]></content>
      <categories>
        <category>基础</category>
      </categories>
      <tags>
        <tag>日志</tag>
      </tags>
  </entry>
  <entry>
    <title>Phoenix索引</title>
    <url>/2022/07/21/Phoenix%E7%B4%A2%E5%BC%95/</url>
    <content><![CDATA[<h3 id="Phoenix简单介绍"><a href="#Phoenix简单介绍" class="headerlink" title="Phoenix简单介绍"></a>Phoenix简单介绍</h3><p>Phoenix是HBase的sql层，基于Phoenix可以通过sql命令操作HBase，降低了学习HBase的成本，同时方便与代码迁移，之前面向关系型数据库的代码，只需要换下数据库的连接及驱动即可。</p>
<p>通过Phoenix创建表，必须制定一个主键，内部实际上会以该主键作为rowkey，在HBase中创建一张表。</p>
<p>在基于Phoenix创建表时，如果没有指定列族的话，会默认自动创建一个名字为0列族。  </p>
<p>同时也可以指定列族，指定列族的时候需要注意，主键列不能指定，否则会报错。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"># 会默认将这三列均放置在列族<span class="number">0</span>下</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> test (id <span class="type">integer</span>, name <span class="type">varchar</span>(<span class="number">10</span>), age <span class="type">integer</span>, <span class="keyword">constraint</span> my_pk <span class="keyword">primary</span> key (id));</span><br><span class="line"></span><br><span class="line"># 指定列族名</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> test ( &quot;id&quot; <span class="type">integer</span>, &quot;person&quot;.&quot;name&quot; <span class="type">varchar</span>(<span class="number">10</span>), &quot;person&quot;.&quot;age&quot; <span class="type">integer</span>, <span class="keyword">constraint</span> my_pk <span class="keyword">primary</span> key (id));</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p>Phoenix最有用功能就是为HBase表创建二级索引。在Phoenix表没有二级索引，我们又没有基于主键进行查询的时候，会进行全表扫描，如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719194147590.png" alt="image-20220719194147590"></p>
<p>但是通过Phoenix创建二级索引，可以避免全表扫描，从而提高检索速度。</p>
<h3 id="Phoenix二级索引"><a href="#Phoenix二级索引" class="headerlink" title="Phoenix二级索引"></a>Phoenix二级索引</h3><p>基于Phoenix可以创建二级索引，二级索引主要由以下几种类型：</p>
<h4 id="全局索引"><a href="#全局索引" class="headerlink" title="全局索引"></a>全局索引</h4><p>创建全局索引的命令如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719195321460.png" alt="image-20220719195321460"></p>
<p>创建的全局索引，会在HBase中创建一个真实的表，该索引表的rowkey是原表的索引列和rowkey组合而来的。</p>
<p><strong>当我们对原表进行select查询时，只有索引列中包含要查询的列时，才会走索引，否则走全表扫描。</strong></p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719195907177.png" alt="image-20220719195907177"></p>
<p>全局索引适合用于读多写少的场景，因为索引表是一个真实存在的表，因此每次的更新都会连带着对全局索引表的更新。</p>
<h4 id="本地索引"><a href="#本地索引" class="headerlink" title="本地索引"></a>本地索引</h4><p>创建本地索引的命令如下图所示。  </p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719201051475.png" alt="image-20220719195321460"></p>
<p>本地索引的特点是，不会额外在hbase中创建一张新表，而是在原表中新增一行，用于存储索引数据，如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719201337293.png" alt="image-20220719201337293"></p>
<p>新增的这行数据的rowkey是由原表中的rowkey，索引列组成的。</p>
<p>本地索引适合写多读少的场景，因为索引数据和真实数据都是在同一张表中的。</p>
<p>本地索引的特点就是不管查询的数据索引表中有没有，都会先走索引，如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719201709014.png" alt="image-20220719201709014"></p>
<h4 id="覆盖索引"><a href="#覆盖索引" class="headerlink" title="覆盖索引"></a>覆盖索引</h4><p>所谓覆盖索引就是，就是把原数据再索引表中也存储一份，这样仅扫描索引表就可以读取到我们所需要的全部数据，不必再对原表进行扫描，只有从索引表中拿不到的数据，才会去原表中获取，从而提高检索速度。</p>
<p>覆盖索引同样也会在HBase中创建一张真实的表，创建覆盖索引的命令如下，通过<code>include</code>关键字，将原表中我们需要的列写入到索引表中。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719202149788.png" alt="image-20220719202149788"></p>
<p>覆盖索引的特点，只有基于索引列的查询才会走索引，否则全部扫描，另外能从索引中全部数据的话，就不会去原表再次读取数据。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719202919611.png" alt="image-20220719202919611"></p>
<h3 id="删除索引"><a href="#删除索引" class="headerlink" title="删除索引"></a>删除索引</h3><p>删除索引命令如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220719200242734.png" alt="image-20220719200242734"></p>
]]></content>
      <categories>
        <category>phoenix</category>
      </categories>
      <tags>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>archlinux安装部署记录</title>
    <url>/2023/11/15/archlinux%E5%AE%89%E8%A3%85%E9%83%A8%E7%BD%B2%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p>&#x2F;&#x2F;TODO</p>
]]></content>
      <categories>
        <category>差生文具多</category>
      </categories>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>hadoop2.7.2集群搭建</title>
    <url>/2022/07/08/hadoop2-7-2%E9%9B%86%E7%BE%A4%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>这里搭建hadoop集群，使用了三台电脑，分别是：  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">主机名分别是：master,salve1,salve2</span></span><br><span class="line">192.168.0.11  </span><br><span class="line">192.168.0.12  </span><br><span class="line">192.168.0.13</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="配置hosts各个主机"><a href="#配置hosts各个主机" class="headerlink" title="配置hosts各个主机"></a>配置hosts各个主机</h2><p>修改hosts文件：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">127.0.0.1 localhost</span><br><span class="line">192.168.0.11    master</span><br><span class="line">192.168.0.12    slave1</span><br><span class="line">192.168.0.13    slave2</span><br></pre></td></tr></table></figure>
<h2 id="免密登录"><a href="#免密登录" class="headerlink" title="免密登录"></a>免密登录</h2><p>安装ssh服务，这里使用的centos环境，需要安装：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#安装ssh服务端</span><br><span class="line">yum install openssh-server -y</span><br><span class="line">#安装ssh客户端</span><br><span class="line">yum install openssh-clients -y</span><br></pre></td></tr></table></figure>
<p>使用命令:<code>ssh-keygen</code>生成本机密钥和公钥，这些文件在{USER_HOME}&#x2F;.ssh目录下，进入该目录，执行命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#authorized_keys文件若没有，首先创建一个</span><br><span class="line">cat id_rsa.pub &gt;&gt; authorized_keys</span><br></pre></td></tr></table></figure>
<p>将authorize_keys文件分发到其他主机上，并追加到对应的authorized_keys文件中。即可实现在master主机上，免密登录其他主机。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ssh slave1   //免密登录</span><br></pre></td></tr></table></figure>

<h2 id="安装jdk"><a href="#安装jdk" class="headerlink" title="安装jdk"></a>安装jdk</h2><p>下载jdk安装包：jdk-XXX.tar.gz,解压安装  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">tar zxvf jdk-XXX.tar.gz -C /opt</span><br><span class="line"></span><br><span class="line">#修改/etc/profile，添加如下语句</span><br><span class="line">export JAVA_HOME=/opt/jdk-XXX</span><br><span class="line">export PATH=&#123;JABA_HOME&#125;/bin:$&#123;PATH&#125;</span><br><span class="line"></span><br><span class="line">source /etc/profile</span><br></pre></td></tr></table></figure>

<h2 id="安装hadoop"><a href="#安装hadoop" class="headerlink" title="安装hadoop"></a>安装hadoop</h2><p>下载hadoop安装包：hadoop-2.7.1.XXX.tar.gz,解压进行安装：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">tar zxvf hadoop-2.7.1.XXX.tar.gz -C /opt</span><br></pre></td></tr></table></figure>

<p>进入hadoop安装目录，修改etc&#x2F;hadoop目录下的各个配置文件：<br>修改core-site.xml文件：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">        &lt;name&gt;fs.defaultFS&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;hdfs://master:9002&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">        &lt;name&gt;fs.default.name&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;hdfs://master:9000&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">        &lt;name&gt;hadoop.tmp.dir&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;file:/home/hadoop/tmp&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>

<p>修改hdfs-site.xml文件：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;</span><br><span class="line">   &lt;property&gt;</span><br><span class="line">        &lt;name&gt;dfs.namenode.secondary.http-address&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;master:9001&lt;/value&gt;</span><br><span class="line">   &lt;/property&gt;</span><br><span class="line">   &lt;property&gt;</span><br><span class="line">         &lt;name&gt;dfs.namenode.name.dir&lt;/name&gt;</span><br><span class="line">         &lt;value&gt;file:/home/hadoop/dfs/name&lt;/value&gt;</span><br><span class="line">   &lt;/property&gt;</span><br><span class="line">   &lt;property&gt;</span><br><span class="line">          &lt;name&gt;dfs.datanode.data.dir&lt;/name&gt;</span><br><span class="line">          &lt;value&gt;file:/home/hadoop/dfs/data&lt;/value&gt;</span><br><span class="line">   &lt;/property&gt;</span><br><span class="line">   &lt;property&gt;</span><br><span class="line">           &lt;name&gt;dfs.replication&lt;/name&gt;</span><br><span class="line">           &lt;value&gt;3&lt;/value&gt;</span><br><span class="line">   &lt;/property&gt;</span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>

<p>修改mapred-site.xml文件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">        &lt;name&gt;mapreduce.framework.name&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;yarn&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">        &lt;name&gt;mapreduce.jobhistory.address&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;master10020&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">        &lt;name&gt;mapreduce.jobhistory.webapp.address&lt;/name&gt;</span><br><span class="line">        &lt;value&gt;master:19888&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>

<p>修改yarn-site.xml文件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;configuration&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.nodemanager.aux-services&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;mapreduce_shuffle&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.nodemanager.aux-services.mapreduce.shuffle.class&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;org.apache.hadoop.mapred.ShuffleHandler&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.resourcemanager.address&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;master:8032&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.resourcemanager.scheduler.address&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;master:8030&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.resourcemanager.resource-tracker.address&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;master:8031&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.resourcemanager.admin.address&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;master:8033&lt;/value&gt;</span><br><span class="line"></span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">    &lt;property&gt;</span><br><span class="line">            &lt;name&gt;yarn.resourcemanager.webapp.address&lt;/name&gt;</span><br><span class="line">            &lt;value&gt;master:8088&lt;/value&gt;</span><br><span class="line">    &lt;/property&gt;</span><br><span class="line">&lt;/configuration&gt;</span><br></pre></td></tr></table></figure>

<p>修改slaves文件,用于指定slave节点：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">slave1 </span><br><span class="line">slave2</span><br></pre></td></tr></table></figure>

<p>修改hadoop-env.sh文件：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#将export JAVA_HOME行修改成如下：</span><br><span class="line">export JAVA_HOME=/opt/jdk.XXX</span><br></pre></td></tr></table></figure>

<p>修改&#x2F;etc&#x2F;profile文件：   </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export HADOOP_HOME=/opt/hadoop-2.7.1</span><br><span class="line">export PATH=&#123;HADOOP_HOME&#125;/bin:$&#123;JAVA_HOME&#125;/bin:$&#123;PATH&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#指令命令：</span><br><span class="line">source /etc/profile</span><br></pre></td></tr></table></figure>

<p>到此，hadoop已经配置成功了，通过scp命令将hadoop解压包分发到各个节点上：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scp /opt/hadoop-2.7.1 slave1@salve1:/opt</span><br><span class="line">scp /opt/hadoop-2.7.1 slave1@salve1:/opt</span><br></pre></td></tr></table></figure>
<p>到此，hadoop集群搭建完毕，开始进行hdfs文件系统的格式化操作：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hadoop namenode -format</span><br></pre></td></tr></table></figure>
<p>当出现如下日志信息时，表示格式化hdfs成功：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">Storage directory /home/hadoop/dfs/name has been successfully formated.</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<h2 id="测试hadoop集群"><a href="#测试hadoop集群" class="headerlink" title="测试hadoop集群"></a>测试hadoop集群</h2><p>在hdfs上新建一个文件夹&#x2F;input,并上传测试文件word.txt:  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hdfs dfs -mkdir  /input</span><br><span class="line"></span><br><span class="line">hdfs dfs -put /home/word.txt /input</span><br></pre></td></tr></table></figure>

<p>使用hadoop自带的wordcount类来统计word.txt中的字数：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hadoop jar /opt/hadoop-2.7.1/share/hadoop/mapreduce/hadoop-mapreduce-examples-2.7.1.XX.jar worcount /input/* /out  </span><br><span class="line"></span><br><span class="line">使用如下指令查看统计结果：</span><br><span class="line">hdfs dfs -cat /out/*</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>hadoop</category>
      </categories>
      <tags>
        <tag>bigdata</tag>
      </tags>
  </entry>
  <entry>
    <title>hbase事务</title>
    <url>/2022/08/16/hbase%E4%BA%8B%E5%8A%A1/</url>
    <content><![CDATA[<h3 id="综述"><a href="#综述" class="headerlink" title="综述"></a>综述</h3><p>HBase支持行级事务，也即用户对于HBase的一条数据的读写，HBase可以保证其原子性。对于多行事务，HBase并不能保证其原子性。</p>
<p>为了保证行级事务，需要考虑写写控制和读写控制，对于读读操作，由于不涉及到数据的修改，因此无须进行并发控制。如果不考虑这两种控制，可能会出现数据不一致情况，也就不是事务了。</p>
<p>下面对于写写控制和读写控制，分别考虑。</p>
<span id="more"></span>

<h3 id="写写控制"><a href="#写写控制" class="headerlink" title="写写控制"></a>写写控制</h3><h4 id="无并发控制"><a href="#无并发控制" class="headerlink" title="无并发控制"></a>无并发控制</h4><p>先考虑不进行并发控制的情况。</p>
<p>假设有两个写线程，同一时间段内，对一行数据进行修改，由于HBase的写入需要经过好两个步骤：</p>
<ul>
<li>写WAL</li>
<li>写入memstore</li>
</ul>
<p>如果不进行并发，那么在更新完WAL之后，在更新memstore时，可能第二个写线程先更新info:company,第一个写线程后更新info:company； 第一个线程先更新info:role，第二个线程后更新info:role，如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/7effbc3f-0c1c-4135-9dc3-f310e9c8b652" alt="img"></p>
<p>那么我们最终读取到的数据就是下面这样，即数据出现不一致。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/1194e8c1-7a84-4da1-9953-1a6d74d07a4f" alt="img"></p>
<h4 id="并发控制"><a href="#并发控制" class="headerlink" title="并发控制"></a>并发控制</h4><p>为了解决上面的问题，HBase引入了行锁机制，以rowkey为锁，当写线程需要更新数据时，需要先获取行锁，然后才能更新，对于没有获取到行锁的写线程，只能等待，直至获取到行锁。具体的流程如下。</p>
<ul>
<li>写操作开始</li>
<li>获取行锁</li>
<li>更新WAL</li>
<li>更新memstore</li>
<li>释放锁</li>
</ul>
<h3 id="读写控制"><a href="#读写控制" class="headerlink" title="读写控制"></a>读写控制</h3><h4 id="无并发控制-1"><a href="#无并发控制-1" class="headerlink" title="无并发控制"></a>无并发控制</h4><p>读写场景，同样先考虑不进行并发控制，可能会出现的情况。</p>
<p>假如现在有两个写线程要更新同一行数据，同时一个读线程要对该行数据进行读取操作。读线程读取的时机是红线部分，此时第一个写线程已经完成，第二个写线程更新了一部分，如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/30530bc9-0305-47a1-a5c2-86281119799c" alt="img"></p>
<p>那么读线程读取的数据就是下面这样子，同样会出现数据不一致情况。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/1194e8c1-7a84-4da1-9953-1a6d74d07a4f" alt="img"></p>
<h4 id="并发控制-1"><a href="#并发控制-1" class="headerlink" title="并发控制"></a>并发控制</h4><p>为了解决上面的问题，HBas引入了MVCC机制，采用无锁机制，实现读写控制。为了实现该机制，HBase在进行写操作时，会进行如下操作：</p>
<ul>
<li>获取行锁</li>
<li>分配一个写事务id</li>
<li>更新WAL</li>
<li>更新memstore</li>
<li>完成写事务id</li>
<li>释放锁</li>
</ul>
<p>与此同时，对于每一次的读操作，HBase都会为其分配一个已经完成、且值最大的写事务id。</p>
<p>如图所示，两个写线程开启写事务时，HBase分别为其分配一个写id，当读事务开启时，由于写事务1已经完成，而写事务2还未完成，因此分配的已完成、其值最大的写事务id是1，称之为read point。因此对该读事务而言，所有小于等于read point的数据都是对其可见的，大于read point的数据是不可见的。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220815203852275.png" alt="image-20220815203852275"></p>
<p>因此，在这种情况下，读线程最终读取的结果是这个样子。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/12b2c8a4-551b-4aa5-8629-874e220a91bd" alt="img"></p>
<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul>
<li><a href="https://blogs.apache.org/hbase/entry/apache_hbase_internals_locking_and"><strong>Apache HBase Internals: Locking and Multiversion Concurrency Control</strong></a></li>
<li><a href="http://hbasefly.com/2017/07/26/transaction-2/"><strong>数据库事务系列－HBase行级事务模型</strong></a></li>
</ul>
]]></content>
      <categories>
        <category>hbase</category>
      </categories>
      <tags>
        <tag>bigdata</tag>
        <tag>事务</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2022/07/07/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>hbase的MOB文件清理机制</title>
    <url>/2024/11/09/hbase%E7%9A%84MOB%E6%96%87%E4%BB%B6%E6%B8%85%E7%90%86%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h4 id="清理MOB文件的线程"><a href="#清理MOB文件的线程" class="headerlink" title="清理MOB文件的线程"></a>清理MOB文件的线程</h4><p>MOB表中的mob文件有时候也需要清理，当MOB文件满足如下条件时，需要对其进行清理。  </p>
<ul>
<li>MOB文件中的数据已经过期无用，此时该MOB文件就可以被清理删除</li>
<li>若干个小的MOB文件被合并之后，老的MOB文件不再被任何region所引用，此时也可以被清理删除</li>
</ul>
<p>针对MOB文件的清理，HBase提供了两个线程任务来执行，分别是<code>RSMobFileCleanerChore</code>和<code>MobFileCleanerChore</code>,两者的特点如下。</p>
<ul>
<li><code>MobFileCleanerChore</code><ul>
<li>调用者：HMaster服务调用执行</li>
<li>范围：针对整个HBase集群的MOB文件进行清理</li>
<li>清理对象： 创建时间超过TTL时间的MOB文件</li>
</ul>
</li>
<li><code>RSMobFileCleanerChore</code><ul>
<li>调用者： RegionServer</li>
<li>范围： 仅清理位于当前RegionServer上的Region所引用的MOB文件</li>
<li>清理对象：没有被任何region引用的MOB文件，属于被清理的对象</li>
</ul>
</li>
</ul>
<span id="more"></span>

<p>下面针对这两个MOB清理任务，分别进行详细说明。</p>
<h4 id="MobFileCleanerChore工作机制"><a href="#MobFileCleanerChore工作机制" class="headerlink" title="MobFileCleanerChore工作机制"></a><code>MobFileCleanerChore</code>工作机制</h4><h5 id="前提条件"><a href="#前提条件" class="headerlink" title="前提条件"></a>前提条件</h5><p>只有满足如要条件的列族，<code>MobFileCleanerChore</code>才会对其执行清理操作</p>
<ul>
<li>列族是MOB类型</li>
<li>列族配置的最小保存版本数为0，如果不为0，则不执行</li>
<li>列族配置了TTL</li>
</ul>
<p>如下所示。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 检查列族是否是MOB类型， 最小保存的版本数是否为0</span></span><br><span class="line"><span class="keyword">if</span> (hcd.isMobEnabled() &amp;&amp; hcd.getMinVersions() == <span class="number">0</span>) &#123;</span><br><span class="line">	cleaner.cleanExpiredMobFiles(htd.getTableName().getNameAsString(), hcd);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断TTL</span></span><br><span class="line"><span class="type">long</span> <span class="variable">timeToLive</span> <span class="operator">=</span> columnDescriptor.getTimeToLive();  </span><br><span class="line"><span class="keyword">if</span> (Integer.MAX_VALUE == timeToLive) &#123;  </span><br><span class="line">  <span class="comment">// no need to clean, because the TTL is not set.  </span></span><br><span class="line">  <span class="keyword">return</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="MobFileCleanerChore的判定条件"><a href="#MobFileCleanerChore的判定条件" class="headerlink" title="MobFileCleanerChore的判定条件"></a><code>MobFileCleanerChore</code>的判定条件</h5><h6 id="获取过期时间"><a href="#获取过期时间" class="headerlink" title="获取过期时间"></a>获取过期时间</h6><p><code>MobFileCleanerChore</code>任务是周期性任务，默认情况下，一天执行一次。<br>在执行时，会首先获取当前时间<code>current</code>，然后根据列族的TTL时间，计算一个过期时间，然后MOB文件时间在这个过期时间之前的，都认为是已经过期的文件，是可以被删除的。<br>如现在时间是<code>2024-09-26 12:00:00</code>, TTL是1天，则计算的过期时间是<code>2024-09-26 12:00:00</code>，然后再对时分秒清零，因此过期时间是<code>2024-09-26 00:00:00+000</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Calendar</span> <span class="variable">calendar</span> <span class="operator">=</span> Calendar.getInstance();  </span><br><span class="line">calendar.setTimeInMillis(current - timeToLive * <span class="number">1000</span>);  </span><br><span class="line">calendar.set(Calendar.HOUR_OF_DAY, <span class="number">0</span>);  </span><br><span class="line">calendar.set(Calendar.MINUTE, <span class="number">0</span>);  </span><br><span class="line">calendar.set(Calendar.SECOND, <span class="number">0</span>);  </span><br><span class="line">calendar.set(Calendar.MILLISECOND, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>这里需要说明下为什么要对时分秒清零？<br>因为MOB文件名中包含时间字符串，但是该字符串是不包含时分秒等数据。</p>
</blockquote>
<h6 id="获取MOB文件的时间"><a href="#获取MOB文件的时间" class="headerlink" title="获取MOB文件的时间"></a>获取MOB文件的时间</h6><p>MOB文件名的格式如下所示。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">d41d8cd98f00b204e9800998ecf8427e20240925221150b7685c4bf5bcba54c97658e3dd_63f50cb1b677b063afc5bcd56c703e2e</span><br></pre></td></tr></table></figure>
<p>其中文件名字符串的<strong>32-39</strong>位表示的是该文件的创建时间，HBase通过解析该文件名字符串获取到MOB文件的创建时间。</p>
<h6 id="MOB文件的时间来源"><a href="#MOB文件的时间来源" class="headerlink" title="MOB文件的时间来源"></a>MOB文件的时间来源</h6><p>MOB数据，在写入的时候也是会首先写入到Memstore，只是在flush的时候，会将元数据信息存放在hfile中，真实的数据会存储在MOB文件中。<br>HBase在flush的时候，会根据暂存在memstore中的KV数据的时间戳来确定MOB文件的时间，如果存在多个KV，则以最新的时间戳最为生成MOB文件的时间。</p>
<p>示例：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1727061887ms = 1970-01-01 08:00:02</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1727061887000ms = 2024-09-23 11:24:47</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">1727061889ms = 1970-01-21 07:44:21</span></span><br><span class="line">put &#x27;mob&#x27;, &#x27;r1&#x27;, &#x27;info:name&#x27;, bigdata_value, 1727061887</span><br><span class="line">put &#x27;mob&#x27;, &#x27;r2&#x27;, &#x27;info:name&#x27;, bigdata_value, 1727061887000</span><br><span class="line">put &#x27;mob&#x27;, &#x27;r3&#x27;, &#x27;info:name&#x27;, bigdata_value, 1727061889</span><br></pre></td></tr></table></figure>

<p>如上所示，我们往mob表中写入了3个KV，其中最新的时间戳是第二个，时间是<code>1727061887000</code>, 因此生成的MOB文件时间就是<code>*20240923*</code>。</p>
<h6 id="对比时间"><a href="#对比时间" class="headerlink" title="对比时间"></a>对比时间</h6><p>由于MOB文件时间是当前文件中最新KV的时间戳，因此只要该时间过期，那么意味着该文件中的所有数据都是过期的，因此都可以删除。<br>通过过期时间和MOB文件时间的对比，如果MOB文件时间处于过期时间之前的，则认为过期。</p>
<blockquote>
<p>比较时间的时候，忽略时分秒是否会删除未过期的数据？<br>答案是不会。</p>
<p>原因：<br>对于MOB过期文件，hbase是允许多存一些时间的。<br>比较过期时间时，忽略了时分秒，意味着是以天为单位来进行比较的；且比较时是将早于过期时间的MOB文件判定为过期<code>fileDate &lt; 过期时间</code>，这意味着即使当天过期的MOB文件，也会继续保留一段时间，在第二天才会被删除，因此不会删除未过期的时间。</p>
<p>举例说明：<br>ttl&#x3D;1s，MOB文件时间是<code>2024-10-20</code>，假设已经过期, <code>MobFileCleanerChore</code>在<code>2024-10-20 12:00:00</code>开始运行，计算出的过期时间是<code>2024-10-20 00:00:00</code>，由于<code>fileDate &lt; 过期时间</code>这个条件不成立，因此本次运行不会将其认定为过期。 只有在<code>2024-10-21 00:00:01</code>时刻及之后，该任务运行，才会判定该MOB文件过期。 </p>
</blockquote>
<h4 id="RSMobFileCleanerChore工作机制"><a href="#RSMobFileCleanerChore工作机制" class="headerlink" title="RSMobFileCleanerChore工作机制"></a><code>RSMobFileCleanerChore</code>工作机制</h4><h5 id="前提条件-1"><a href="#前提条件-1" class="headerlink" title="前提条件"></a>前提条件</h5><p>只有满足如下如要条件的列族，<code>RSMobFileCleanerChore</code>才会对其执行清理操作 </p>
<ul>
<li>列族是MOB类型</li>
</ul>
<p>代码如下所示。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;ColumnFamilyDescriptor&gt; list = MobUtils.getMobColumnFamilies(htd);</span><br></pre></td></tr></table></figure>

<h5 id="RSMobFileCleanerChore的执行流程"><a href="#RSMobFileCleanerChore的执行流程" class="headerlink" title="RSMobFileCleanerChore的执行流程"></a><code>RSMobFileCleanerChore</code>的执行流程</h5><ol>
<li>获取当前RegionServer上的表，针对每个表，有如下操作</li>
<li>遍历每个表的Region</li>
<li>针对每个Region，遍历其所有列族<ol>
<li>找到该region下的MOB列族，遍历列族下的所有hfile文件，并进行如下操作<ol>
<li>读取hfile，获取元数据信息</li>
<li>元数据信息获取该hfile文件是否引用有MOB文件（原因是MOB列族中也可能写入小数据，此时是不生成MOB文件的）</li>
<li>如果hfile文件引用有MOB文件，则记录该MOB文件信息；没有继续进行下一个hfile文件的判断</li>
</ol>
</li>
</ol>
</li>
<li>针对一个表，记录完毕所有满足条件的MOB文件信息</li>
<li>再次针对这个表的MOB列族，进行如下操作</li>
<li>遍历每个MOB列族</li>
<li>针对每个MOB列族，获取对应的mobdir目录下的所有mob文件，针对文件，进行如下操作<ol>
<li>根据文件名信息获取引用该MOB文件的region name</li>
<li>基于region name判断该region是否在当前的这个RegionServer上，如果不在，跳过该MOB文件；如果在，进行下一步操作</li>
<li>判断该MOB文件是否被region引用（和上面的记录信息比较），如果存在，则跳过该MOB文件；如果不存在，则说明没有引用，可以进行下一步操作</li>
<li>判断该MOB文件的最后更新时间是否超过阈值（默认是1h），如果超过，则将其归档清理；不超过，则不归档</li>
</ol>
</li>
</ol>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>虽然HBase中存在两类清理MOB文件的线程，<code>RSMobFileCleanerChore</code>和<code>MobFileCleanerChore</code>。<br>但是两者的作用是完全不同的。</p>
<ul>
<li><code>MobFileCleanerChore</code>线程，主要用于清理TTL过期的MOB文件，针对的是设置有TTL配置的MOB列族</li>
<li><code>RSMobFileCleanerChore</code>线程，主要用于清理无用的MOB文件，针对的是MOB表中的所有MOB列族。</li>
</ul>
]]></content>
      <categories>
        <category>hbase</category>
      </categories>
      <tags>
        <tag>bigdata</tag>
      </tags>
  </entry>
  <entry>
    <title>linux中很有用的命令</title>
    <url>/2022/07/08/linux%E4%B8%AD%E5%BE%88%E6%9C%89%E7%94%A8%E7%9A%84%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p>记录一些常用的Linux指令。</p>
<h2 id="dos2unix"><a href="#dos2unix" class="headerlink" title="dos2unix"></a>dos2unix</h2><p>把Windows上创建的文件，拷贝到Linux机器上，通常会出现文件格式不正确问题，从而导致会出现一些问题，最明显的就是shell脚本，这事因为Windows中的换行符是<code>\r\n</code>,而在Linux中，文件换行符是<code>\n</code></p>
<ul>
<li>Windows中的文件格式为dos  </li>
<li>Linux中的文件格式为unix</li>
</ul>
<span id="more"></span>

<p>为了把dos文件转换成unix，可以使用dos2unix工具，不仅可以对单个文件进行转换，还可以批量转换，很好用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#单个文件转换</span><br><span class="line">dos2unix fileName</span><br><span class="line"></span><br><span class="line">#批量转换</span><br><span class="line">find ./ -type f | xargs dos2unix</span><br></pre></td></tr></table></figure>

<h2 id="sed"><a href="#sed" class="headerlink" title="sed"></a>sed</h2><p>sed可以对文件进行编辑，功能非常多，能够满足几乎所有的对文件的操作  </p>
<ul>
<li><p>1 替换文本内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#将文本test.txt中的所有old字符串替换成new字符串</span><br><span class="line">sed -i &#x27;s:old:new:g&#x27; test.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p>2 删除匹配行</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#将文本中的包含“delete_conent”字符串的行删除掉</span><br><span class="line">sed -i &#x27;/delete_conent/d&#x27; test.txt</span><br><span class="line"></span><br><span class="line">##删除匹配行的下一行</span><br><span class="line">sed -ni &#x27;p;/target_content/n&#x27; test.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p>3 插入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#在匹配行的下一行添加新内容，，，a代表的是after</span><br><span class="line">sed -i &#x27;/target_conent/a\new_add_content&#x27; test.txt</span><br><span class="line"></span><br><span class="line">#在匹配行的上一行添加新内容，，，i代表的是in front</span><br><span class="line">sed -i &#x27;/target_content/i\new_add_content&#x27; test.txt</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>shell</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql中的mvcc</title>
    <url>/2022/08/16/mysql%E4%B8%AD%E7%9A%84mvcc/</url>
    <content><![CDATA[<h3 id="综述"><a href="#综述" class="headerlink" title="综述"></a>综述</h3><p>MVCC全称多版本并发控制。</p>
<p>主要原理就是对于同一份数据，在数据库中存储多份，每一份都是一个版本。然后根据事务发生的先后顺序，来决定哪些版本的数据对该事务可见，从而实现并发控制。</p>
<p><strong>MVCC主要解决的问题</strong></p>
<p>MVCC主要解决的是读写并发问题，可以在无锁情况下实现读写并发，从而可以提交读写效率。</p>
<p>关系型数据库中的读已提交、可重复读事务的隔离等级就是基于MVCC实现的。</p>
<span id="more"></span>
<h3 id="MVCC在mysql中的应用"><a href="#MVCC在mysql中的应用" class="headerlink" title="MVCC在mysql中的应用"></a>MVCC在mysql中的应用</h3><p>mysql中的Innodb存储引擎支持MVCC，对于Innodb表，mysql会默认为表创建几个隐藏列，其中比较重要的就是</p>
<ul>
<li><p>tx_id ,事务id列，用于表明修改该行数据的事务id</p>
</li>
<li><p>回滚指针列，由于Innodb支持事务，且默认事务的隔离等级为可重复读，其内部对于一行数据，会存储多个版本，回滚指针用于指向在事务修改该行数据之前，该行数据的样子(即该行数据的上一个版本)，如下图所示，图片来自 <a href="https://segmentfault.com/a/1190000037557620">MySQL的多版本并发控制(MVCC)是什么</a></p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/bVbyzV9" alt="Undo Log回滚历史记录"></p>
</li>
</ul>
<h4 id="mvcc实现可重复读"><a href="#mvcc实现可重复读" class="headerlink" title="mvcc实现可重复读"></a>mvcc实现可重复读</h4><p>mysql会按照事务的先后顺序，以严格递增方式为每一个事务分配一个唯一的事务id。</p>
<p>当mysql开启一个事务时，会立即形成一个快照，称之为<code>read view</code>,<code>read view</code>中记录了所有当前活跃的事务id(<strong>所谓活跃，是指事务已经开始，但是还没有结束</strong>)。在<code>read view</code>中的活跃事务集合中，存在一个最小id的事务，假设称之为<code>low_tx_id</code>, 存在一个最大事务id，<code>high_tx_id</code>。当前事务称之为<code>cur_tx_id</code>，假定<code>cur_tx_id</code>要读取的那行数据的隐藏事务列为<code>tx_id</code>，则可能存在如下几种情况。</p>
<blockquote>
<p><strong>这里需要说明一点，当前事务id不一定是<code>read view</code>中的最大值，因为在开启事务时，生成<code>read view</code>之前，可能会有新的事务进来，这个时候就会出现<code>read view</code>中的<code>rv_max_tx_id</code>比当前事务id大的情况</strong></p>
</blockquote>
<ul>
<li><code>cur_tx_id = tx_id</code>，表示这行数据就是由当前事务修改的，必然对该事务可见</li>
<li><code>tx_id &lt; low_tx_id</code>，表示在当前事务开启的时候，<code>tx_id</code>所代表的事务已经提交了，因此这行的该版本数据，对当前事务可见</li>
<li><code>low_tx_id &lt;= tx_id  &lt;= high_tx_id</code>, 且<code>tx_id</code>在<code>read view</code>集合中，表示当前事务开启时，<code>tx_id</code>事务还未提交，因此这行数据的这个版本对当前事务不可见</li>
<li>l<code>ow_tx_id &lt;= tx_id  &lt;= high_tx_id</code>, 且<code>tx_id</code>不在<code>read view</code>集合中，表示当前事务开启时，<code>tx_id</code>事务已经提交，因此这行数据对当前事务可见</li>
<li><code>tx_id &gt; high_tx_id</code>，表示<code>tx_id</code>事务是在当前事务开启之后提交的，因此数据对当前事务不可见</li>
</ul>
<p>mvcc通过如上原理，再加上回滚指针，按照如下流程就行读取。</p>
<ul>
<li><p>当读取的数据版本，对当前事务可见时，则可以直接读取，结束</p>
</li>
<li><p>当读取的数据版本，对当前事务不可见时，则按照改行的隐藏列回滚指针列，去读取这上数据的上一个版本（这个数据是存储在undo日志中的）</p>
</li>
<li><p>首先读取这个版本对应的事务id列，获取<code>tx_id</code>，并与上述规则进行匹配，成功，则该版本数据对当前事务可见，结束； 否则继续第二步</p>
</li>
</ul>
<h4 id="MVCC实现读已提交"><a href="#MVCC实现读已提交" class="headerlink" title="MVCC实现读已提交"></a>MVCC实现读已提交</h4><p>在读提提交隔离级别，MVCC遵循的规则和上面一致，不同的点是，在事务执行过程中，每一次的读操作，都会创建一个<code>read view</code>。</p>
<p>这样的话，<code>read view</code>不断更新，因此之前不可见的数据，随着对应事务的提交，也会对当前事务可见，从而就实现了读已提交。具体流程如下。</p>
<ul>
<li>读事务开启</li>
<li>当要进行读取操作时，立即生成一个<code>read view</code>,并获取对应行数据，安装MVCC规则进行匹配，匹配成功，则该版本数据对当前事务可见，否则根据回滚指针读取上一个版本的数据，直至读取到对当前事务可见的版本</li>
<li>其他操作</li>
<li>当再次涉及到读操作时，继续进行第二步操作</li>
</ul>
<h3 id="写写控制"><a href="#写写控制" class="headerlink" title="写写控制"></a>写写控制</h3><p>MVCC实现了读写之前的无锁并发控制，而对于写写操作，Innodb表采用锁机制来进行并发控制，不过它采用的是行锁来进行控制，与表锁相比，它的粒度更细，锁定的范围更小，因此效率更高。</p>
<p><strong>参考：<a href="https://segmentfault.com/a/1190000037557620">MySQL的多版本并发控制(MVCC)是什么？</a>    <a href="https://segmentfault.com/a/1190000037557620">https://segmentfault.com/a/1190000037557620</a></strong></p>
]]></content>
      <categories>
        <category>mysql</category>
      </categories>
      <tags>
        <tag>事务</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql索引</title>
    <url>/2023/08/18/mysql%E7%B4%A2%E5%BC%95/</url>
    <content><![CDATA[<h3 id="索引介绍"><a href="#索引介绍" class="headerlink" title="索引介绍"></a>索引介绍</h3><p>索引是一种特殊的数据结构，其主要目的就是为了加快数据的查询速度。<br>mysql索引基于表的指定字段，按照某种数据结构进行排序，并将数据集存储于实际的文件中。<br>mysql索引中也记录了索引字段和表真实记录之间的映射关系，当我们基于索引查询到数据时，再基于该映射关系，据可以快速定位到真实记录行所在的位置，并实现快速的读取。</p>
<span id="more"></span>

<h5 id="为什么基于索引的查询要比普通查询快"><a href="#为什么基于索引的查询要比普通查询快" class="headerlink" title="为什么基于索引的查询要比普通查询快?"></a>为什么基于索引的查询要比普通查询快?</h5><p>由于索引时按照特定数据结构进行组织的（如B+树、Hash），因此在查询数据的时候，可以在低于O(N)(全部扫描)的方式，查询到目标数据，所以其查询速度就非常快。<br>基于索引的查询速度比较快的最本质原因就是，减少了本次查询的IO次数，从而加快了索引。</p>
<p>举个例子，假设一张mysql表有1000页，我们查询的数据恰巧在第1000页的最后。看看两种查询的效果。   </p>
<ul>
<li>全表扫描：一页一页的顺序遍历，最后在第1000页的最后一行找到我们想要的数据，最后计算查询总共进行了1000次IO  </li>
<li>基于索引的查询： 假设索引结构是一个B+树结构，且层高为3层，那么查询时，首先基于索引进行检索，进行3次IO即可获取目标数据所在的页，然后对该数据页进行加载读取，得到我们需要的数据，最终计算查询总共进行了4次IO</li>
</ul>
<p>对比两种查询方式，可以发现在数据量大的情况下，索引查询性能远远优于全表扫描。</p>
<h5 id="索引的优缺点"><a href="#索引的优缺点" class="headerlink" title="索引的优缺点"></a>索引的优缺点</h5><ul>
<li>优势：在查询数据时，基于索引进行查询速度很快</li>
<li>弊端：mysql索引是真实存在的文件，因此会占用一定的存储空间；在更新表数据的时候，需要同步更新索引树，这会带来额外的开销</li>
</ul>
<h3 id="索引分类"><a href="#索引分类" class="headerlink" title="索引分类"></a>索引分类</h3><p>在mysql中，可以按照不同方式，对索引进行分类。<br>按照索引数据结构可以将索引分为：B+树索引、Hash索引。<br>按照索引中存储的数据，可以将索引分为聚簇索引、辅助索引。<br>按照索引的字段，可以将索引分为单列索引、联合索引。<br>按照索引的字段性质，将以将索引分为主键索引、唯一索引、普通索引</p>
<h4 id="按照数据结构区分索引"><a href="#按照数据结构区分索引" class="headerlink" title="按照数据结构区分索引"></a>按照数据结构区分索引</h4><p>mysql中最常见的索引就是B+树索引（又称为B树索引）、Hash索引。</p>
<blockquote>
<p>B+树索引又叫B树索引，是因为在mysql中，如果索引类型是B+树，显示的结构是:<code>Index_type: BTREE</code>.  </p>
</blockquote>
<p>其中mysql的Innodb和Mylsam存储引擎支持B树索引，memory存储引擎支持Hash索引。</p>
<h5 id="B-树索引"><a href="#B-树索引" class="headerlink" title="B+树索引"></a>B+树索引</h5><p>对指定列中的数据，按照B+树数据结构进行构建，非叶节点用于存储索引信息，叶子节点用于存储真实数据，并且是有序的，这里存储对应的字段数据以及指向真实页的索引。  </p>
<p><em>不仅如此，B+树的叶子节点还会通过双向链表进行连接，以便于进行范围查询。</em></p>
<blockquote>
<p>这里存在一个问题：<br>索引为什么采取B+树，而不是B-树？<br>B+ 树非叶节点用于存储索引信息，叶子节点用于存储真实数据。<br>B-树是所有节点均用于存储真实数据,相比于真实数据，索引数据的size是比较小的，这意味着，B+树的非叶节点可以存储更多的数据，因此可以指向更多的叶子节点。<br>因此在相同树高下，B+树可以存储的信息要远远大于B树，这就意味着对相同数据量而言，构建B-树需要更多的层高，那么在极端情况下，查询的数据在叶子节点上，则需要更多的IO，效率太低。<br>另外B+树的数据均存储在叶子节点上，且有序，用链表将叶子节点连接起来，比较适用于范围查询，而B-树则不行。</p>
</blockquote>
<h5 id="Hash索引"><a href="#Hash索引" class="headerlink" title="Hash索引"></a>Hash索引</h5><p>所谓Hash索引，就是对指定列的数据，按照Hash表数据结果方式进行构建。基于字段值做hash，散列至hash表的某个位置上，Hash表中存储真实的数据。</p>
<h4 id="按照索引中的数据区分索引"><a href="#按照索引中的数据区分索引" class="headerlink" title="按照索引中的数据区分索引"></a>按照索引中的数据区分索引</h4><p>按照此类方式来区分索引，mysql中有两类索引：聚簇索引和辅助索引。</p>
<h5 id="聚簇索引"><a href="#聚簇索引" class="headerlink" title="聚簇索引"></a>聚簇索引</h5><p>以B+树数据结构的索引为例，叶子节点上如果存储的是对应表上的真实记录的话，那么就称该索引为聚簇索引。</p>
<p>对于mysql的Innodb表而言，如果表中存在主键，那么就会基于主键建立一个聚簇索引，如果没有指定主键的话，那么mysql会自动创建一个隐藏的自增字段，并基于该自增字段来建立聚簇索引。</p>
<h5 id="辅助索引"><a href="#辅助索引" class="headerlink" title="辅助索引"></a>辅助索引</h5><p>仍以B树数据结构的索引为例，如果叶子存储的不是对应表中的真实记录，而是存储的是指定字段值与对应表真实记录的映射关系，则称该索引为辅助索引。</p>
<p>通常情况下，我们对表建立的二级索引，称之为辅助索引。</p>
<h4 id="按照索引的字段区分索引"><a href="#按照索引的字段区分索引" class="headerlink" title="按照索引的字段区分索引"></a>按照索引的字段区分索引</h4><p>索引可以针对mysql表中的其中一列，也可以针对mysql表中的多列。根据这个特点，可以将索引分为单列索引、联合索引。</p>
<h5 id="单列索引"><a href="#单列索引" class="headerlink" title="单列索引"></a>单列索引</h5><p>针对表中的其中一列所建立的索引称之为单列索引。</p>
<h5 id="联合索引"><a href="#联合索引" class="headerlink" title="联合索引"></a>联合索引</h5><p>针对表中的多列所建立的索引称之为联合索引。</p>
<ul>
<li>最左匹配<br>针对联合索引的查询，有一个特点，就是遵循最左匹配原则。<br>联合索引是基于多个字段构建的，在构建时，这些字段是有序的。<br>在针对联合索引的查询，必须要匹配上索引字段的最左字段，否则，联合索引不生效。</li>
</ul>
<p>举个例子：联合索引是基于A、B、C字段建立，这三个字段按照ABC排列，在查询的时候，查询条件只有匹配到最左侧的A字段时，联合索引才生效。</p>
<ul>
<li>联合索引为什么要遵循最左匹配原则？<br>这是由联合索引的结构所决定的。<br>联合索引构建时，会首先以多个字段的最左侧字段进行排序，当有多行数据最左侧字段值相同时，此时联合索引才会再根据索引字段列中的下一个字段进行排序，以此类推。<br>因此对于联合索引而言，它是针对最左字段整体有序的，针对其他索引字段是局部有序的。</li>
</ul>
<p>因此在查询的时候，我们必须要满足最左匹配原则，否则无法确定目标数据在索引中的位置。</p>
]]></content>
      <categories>
        <category>mysql</category>
      </categories>
      <tags>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Hexo+Next+github搭建个人博客</title>
    <url>/2022/07/14/%E4%BD%BF%E7%94%A8Hexo-Next-github%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<p>Hexo 是一个快速、简洁且高效的博客框架。Hexo使用Markdown解析文章，在几秒内，即可利用靓丽的主题生成静态网页。这些静态资源文件可以托管在github或者其他服务器，形成我们自己的个人博客网站。</p>
<p>next是Hexo框架内的一套主题。</p>
<p>github作为生成的静态资源文件的托管平台。实际上我们通过Hexo也可以实现这些静态资源的托管，但是这样只能发布在本地，如果想通过互联网发布，则需要自己购买服务器，并自行申请域名之类的操作。而github中提供github pages功能，可以免费托管我们发布的内容，另外github pages还会自动为其创建一个域名。</p>
<span id="more"></span>

<h2 id="hexo框架"><a href="#hexo框架" class="headerlink" title="hexo框架"></a>hexo框架</h2><h3 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h3><p>由于hexo是基于Node.js的框架，因此安装hexo之前，首先需要安装node.js。<br>node.js和hexo之间存在版本要求，具体如下所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220711173853678.png" alt="image-20220711173853678"></p>
<p>待node.js安装之后，可以通过npm命令去安装hexo。</p>
<p>建议使用如下方式进行hexo安装</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<p>安装成功之后，可以通过如下方式验证hexo的效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714134809507.png" alt="image-20220714134809507"></p>
<h3 id="hexo基本操作"><a href="#hexo基本操作" class="headerlink" title="hexo基本操作"></a>hexo基本操作</h3><h4 id="初始化一个hexo项目"><a href="#初始化一个hexo项目" class="headerlink" title="初始化一个hexo项目"></a>初始化一个hexo项目</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">初始化一个hexo 博客</span></span><br><span class="line">hexo init blog_test</span><br></pre></td></tr></table></figure>

<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712134610287.png" alt="image-20220712134610287"></p>
<p>在生成一个hexo_test文件夹后，hexo会自动创建如下内容：</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712134828979.png" alt="image-20220712134828979"></p>
<p>其中比较重要的是：</p>
<ul>
<li><p>source，文件夹，我们的原始文件内容会存放在这里</p>
</li>
<li><p>_config.yml，文件，称之为站点配置文件，用于配置一些全局性的配置</p>
</li>
<li><p>public，文件夹，生成的静态资源文件，会放到public文件夹下，默认按照时间进行分类（初始化时，未创建，在执行完下面的hexo g之后，会自动生成）</p>
</li>
</ul>
<h4 id="生成静态资源"><a href="#生成静态资源" class="headerlink" title="生成静态资源"></a>生成静态资源</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">一键生成静态资源</span></span><br><span class="line">hexo g</span><br></pre></td></tr></table></figure>

<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712135315014.png" alt="image-20220712135315014"></p>
<p>上面的命令会自动生成静态资源文件，并将source下的markdown文件生成为html文件，存放在public文件中，如图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712135737455.png" alt="image-20220712135737455"></p>
<h4 id="启动服务器，发布"><a href="#启动服务器，发布" class="headerlink" title="启动服务器，发布"></a>启动服务器，发布</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">启动hexo服务，用于在本地浏览器查看hexo生成的网页内容</span></span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712140942030.png" alt="image-20220712140942030"></p>
<p>然后打开浏览器，输入<a href="http://localhost:4000/%E5%B0%B1%E5%8F%AF%E4%BB%A5%E8%AE%BF%E9%97%AE%E5%8D%9A%E5%AE%A2%E4%BA%86%E3%80%82">http://localhost:4000/就可以访问博客了。</a></p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712141114021.png" alt="image-20220712141114021"></p>
<h2 id="next主题"><a href="#next主题" class="headerlink" title="next主题"></a>next主题</h2><p>hexo默认的主题，不是太好看，使用比较多的hexo主题是next。</p>
<h3 id="安装next主题"><a href="#安装next主题" class="headerlink" title="安装next主题"></a>安装next主题</h3><p>切换到hexo项目目录，会有一个theme文件夹，用于存放各种主题，hexo默认是没有安装next主题的，我们需要通过git clone从github上将next主题clone到theme文件夹中。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/iissnan/hexo-theme-next themes/next</span><br></pre></td></tr></table></figure>

<p>同时，修改hexo项目的站点配置文件内容</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将主题修改为next</span></span><br><span class="line">theme: next</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">修改语言，由于next中语言包中对应的中文简体为zh-Hans,因此进行如下修改</span></span><br><span class="line">language: zh-Hans</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">title用于标明网站的名字</span></span><br><span class="line">title: guluo</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">author</span></span><br><span class="line">author: 孤落</span><br></pre></td></tr></table></figure>

<p>接下来执行如下命令进行重新发布。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">clean之后重新生成静态文件，然后发布</span></span><br><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo s</span><br></pre></td></tr></table></figure>

<p>此时从浏览器访问，可能会出现如下错误，原因是hexo在5.0之后把swig给删除了需要自己手动安装。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712141716057.png" alt="image-20220712141716057"></p>
<p>此时在项目中安装swig可以解决问题：<code> npm i hexo-renderer-swig</code>，再次重新发布后，登录博客页面。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712143133581.png" alt="image-20220712143133581"></p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712143043445.png" alt="image-20220712143043445"></p>
<p>从博客页面上来看，对站点配置文件的修改配置也生效了</p>
<h3 id="配置next"><a href="#配置next" class="headerlink" title="配置next"></a>配置next</h3><p>next主题下同样有一个_config.yml文件，称之为主题配置文件。通过修改主题配置文件，可以进行一些自定义配置。</p>
<ul>
<li>更改next的scheme。</li>
</ul>
<p>next提供四种scheme，这里我选择的是：<code>scheme: Gemini</code>，修改完配置文件，保存并刷新浏览器，可以查看修改后的效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712143620514.png" alt="image-20220712143620514"></p>
<ul>
<li>设置博客头像</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">配置用户头像</span></span><br><span class="line">avatar: https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/guluo.jpg</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">用户头像下的名字</span></span><br><span class="line">author: 孤落</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">用户头像名字下的说明</span></span><br><span class="line">description: 大漠孤烟直，长河落日圆</span><br></pre></td></tr></table></figure>

<p>修改并保存好主题配置文件后，刷新浏览器，可查看效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712143917357.png" alt="image-20220712143917357"></p>
<ul>
<li>将博客头像设置为圆形</li>
</ul>
<p>方形的用户头像不太好看，可以通过如下修改，将头像修改为圆形的。</p>
<p>切换至<code>next\source\css_common\components\sidebar</code>目录下，将sidebar-author.styl中的内容替换为如下内容。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">.site-author-image &#123;</span><br><span class="line">  margin: 0 auto;</span><br><span class="line">  padding: $site-author-image-padding;</span><br><span class="line">  max-width: $site-author-image-width;</span><br><span class="line">  height: $site-author-image-height;</span><br><span class="line">  border: $site-author-image-border-width solid $site-author-image-border-color;</span><br><span class="line"></span><br><span class="line">  border-radius: 50%;</span><br><span class="line">  -webkit-border-radius: 50%;</span><br><span class="line">  -moz-border-radius: 50%;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.site-author-image:hover &#123;</span><br><span class="line">    -webkit-transform: rotate(360deg);</span><br><span class="line">    -moz-transform: rotate(360deg);</span><br><span class="line">    -ms-transform: rotate(360deg);</span><br><span class="line">    -transform: rotate(360deg);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.site-author-name &#123;</span><br><span class="line">  margin: $site-author-name-margin;</span><br><span class="line">  text-align: $site-author-name-align;</span><br><span class="line">  color: $site-author-name-color;</span><br><span class="line">  font-weight: $site-author-name-weight;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.site-description &#123;</span><br><span class="line">  margin-top: $site-description-margin-top;</span><br><span class="line">  text-align: $site-description-align;</span><br><span class="line">  font-size: $site-description-font-size;</span><br><span class="line">  color: $site-description-color;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>保存，并刷新浏览器查看效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712144235301.png" alt="image-20220712144235301"></p>
<h2 id="博客个性化配置"><a href="#博客个性化配置" class="headerlink" title="博客个性化配置"></a>博客个性化配置</h2><h3 id="为博客增加标签、分类页"><a href="#为博客增加标签、分类页" class="headerlink" title="为博客增加标签、分类页"></a>为博客增加标签、分类页</h3><p>在hexo项目的根目录下，执行如下命令，进行创建：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">创建分类页</span></span><br><span class="line">hexo new page categories </span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">创建标签页</span></span><br><span class="line">hexo new page tags</span><br></pre></td></tr></table></figure>

<p>执行上面的命令后，hexo会在post下自动创建目录，如图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220712170844909.png" alt="image-20220712170844909"></p>
<p>同时更新next主题配置文件,注意要去掉||前面的空格，否则点击页面会跳转失败。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">menu:</span><br><span class="line">  home: /|| home</span><br><span class="line">  tags: /tags/|| tags</span><br><span class="line">  categories: /categories/|| th</span><br><span class="line">  archives: /archives/|| archive</span><br></pre></td></tr></table></figure>

<p>保存配置，并刷新浏览器，查看效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714103032946.png" alt="image-20220714103032946"></p>
<h3 id="增加站内搜索功能"><a href="#增加站内搜索功能" class="headerlink" title="增加站内搜索功能"></a>增加站内搜索功能</h3><p>要实现博客的站内搜索功能，需要借助于插件<code>hexo-generator-searchdb</code>，因此在hexo项目下，安装该插件：  </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>

<p>修改站点配置文件，增加如下内容：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">search:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">search.xml</span></span><br><span class="line">  <span class="attr">field:</span> <span class="string">post</span></span><br><span class="line">  <span class="attr">format:</span> <span class="string">html</span></span><br><span class="line">  <span class="attr">limit:</span> <span class="number">10000</span></span><br></pre></td></tr></table></figure>

<p>然后再更新next主题配置文件：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">local_search:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span>    <span class="comment">#将此设为true</span></span><br><span class="line">  <span class="comment"># if auto, trigger search by changing input</span></span><br><span class="line">  <span class="comment"># if manual, trigger search by pressing enter key or search button</span></span><br><span class="line">  <span class="attr">trigger:</span> <span class="string">auto</span></span><br><span class="line">  <span class="comment"># show top n results per article, show all results by setting to -1</span></span><br><span class="line">  <span class="attr">top_n_per_article:</span> <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>保存，并重启hexo，打开浏览器，查看效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714141357379.png" alt="image-20220714141357379"></p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714141451682.png" alt="image-20220714141451682"></p>
<h3 id="设置博客文章展示部分"><a href="#设置博客文章展示部分" class="headerlink" title="设置博客文章展示部分"></a>设置博客文章展示部分</h3><p>在hexo的source目录下的博客源markdown文件中，合适的位置增加一样md语法，即可实现。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714103321987.png" alt="image-20220714103321987"></p>
<p>在浏览器中的显示效果如图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714103449990.png" alt="image-20220714103449990"></p>
<h2 id="将hexo项目部署到github-page"><a href="#将hexo项目部署到github-page" class="headerlink" title="将hexo项目部署到github page"></a>将hexo项目部署到github page</h2><p>首先得在github上创建一个仓库，仓库名字是<code>&#123;github昵称&#125;.github.io</code>。然后查看该仓库的settings，并选择pages页面，可以查看到网址，我们通过该网址可以直接访问该仓库中的静态html资源。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714104338520.png" alt="image-20220714104338520"></p>
<p>目前还访问不了，原因是这个仓库中没有任何文件，我们需要将前面hexo生成的静态资源上传至该仓库。</p>
<p>为了能够将数据上传至该仓库，我们需要再hexo的站点配置文件中增加有关github的配置。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 这里有个前提，就是本机已经配置了有关github的认证信息</span></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">    <span class="attr">repository:</span> <span class="string">https://github.com/guluo2016/guluo2016.github.io.git</span></span><br><span class="line">    <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>

<p>在hexo项目下执行如下命令，安装hexo的git工具。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>接着执行如下命令，完成在github上的部署。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">清空缓存</span></span><br><span class="line">hexo clean </span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">生成静态资源</span></span><br><span class="line">hexo g</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">部署至github</span></span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>接着，就可以登录前面所述的github pages页面的网址，查看博客网站了。</p>
<h2 id="设置评论功能模块"><a href="#设置评论功能模块" class="headerlink" title="设置评论功能模块"></a>设置评论功能模块</h2><p>这里采用gitalk插件，来实现自建博客的评论功能。</p>
<p>他实际上就是借助于github仓库提供的issue功能来实现的评论功能，如果我们增加了gitalk插件，那么他会为博客中的所有文章在指定仓库的issue上创建一个以该博客文章名命名的问题，后面所有任何关于该博客文章的评论都会记录在这个issue下。</p>
<p>同时当我们打开这个博客文章时，gitalk会自动从issue中加载数据，将关于这个博客的评论展示在评论区。</p>
<p>为了实现上述功能，需要进行如下操作。</p>
<h3 id="申请OAuth-认证"><a href="#申请OAuth-认证" class="headerlink" title="申请OAuth 认证"></a>申请OAuth 认证</h3><p>登录github，在个人账户settings下，选择 <code>Developer settings</code> – <code>OAuth Apps</code>,进行OAuth认证。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714110420653.png" alt="image-20220714110420653"></p>
<p>注意homepageurl和callback url都填写前面的github page网址。</p>
<p>接着通过如下方式，获取到client id和ClientSecret，后面会用到。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714110622403.png" alt="image-20220714110622403"></p>
<h3 id="在next主题配置文件中配置gitalk"><a href="#在next主题配置文件中配置gitalk" class="headerlink" title="在next主题配置文件中配置gitalk"></a>在next主题配置文件中配置gitalk</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">gitalk:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">githubID:</span> <span class="string">guluo2016</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">guluo2016.github.io</span>  <span class="comment">#指定存放评论的仓库名，评论存放在仓库的issue上，因此仓库必须开启issue功能才行</span></span><br><span class="line">  <span class="attr">ClientID:</span> &#123;<span class="string">clientid</span>&#125;</span><br><span class="line">  <span class="attr">ClientSecret:</span> &#123;<span class="string">ClientSecret</span>&#125;</span><br><span class="line">  <span class="attr">adminUser:</span> <span class="string">guluo2016</span> <span class="comment">#指定可初始化评论账户</span></span><br><span class="line">  <span class="attr">distractionFreeMode:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<h3 id="增加gitalk相关内容"><a href="#增加gitalk相关内容" class="headerlink" title="增加gitalk相关内容"></a>增加gitalk相关内容</h3><p>在<code>netx\layout\_third-party\comments</code>目录下，新建gitalk.swig文件，并添加如下内容</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">&#123;% if page.comments &amp;&amp; theme.gitalk.enable %&#125;</span><br><span class="line">  &lt;link rel=&quot;stylesheet&quot; href=&quot;https://unpkg.com/gitalk/dist/gitalk.css&quot;&gt;</span><br><span class="line">  &lt;script src=&quot;/js/src/md5.min.js&quot;&gt;&lt;/script&gt;</span><br><span class="line">  &lt;script src=&quot;https://unpkg.com/gitalk/dist/gitalk.min.js&quot;&gt;&lt;/script&gt;</span><br><span class="line">   &lt;script type=&quot;text/javascript&quot;&gt;</span><br><span class="line">        var gitalk = new Gitalk(&#123;</span><br><span class="line">          clientID: &#x27;&#123;&#123; theme.gitalk.ClientID &#125;&#125;&#x27;,</span><br><span class="line">          clientSecret: &#x27;&#123;&#123; theme.gitalk.ClientSecret &#125;&#125;&#x27;,</span><br><span class="line">          repo: &#x27;&#123;&#123; theme.gitalk.repo &#125;&#125;&#x27;,</span><br><span class="line">          owner: &#x27;&#123;&#123; theme.gitalk.githubID &#125;&#125;&#x27;,</span><br><span class="line">          admin: [&#x27;&#123;&#123; theme.gitalk.adminUser &#125;&#125;&#x27;],</span><br><span class="line">          id: md5(location.pathname),</span><br><span class="line">          distractionFreeMode: &#x27;&#123;&#123; theme.gitalk.distractionFreeMode &#125;&#125;&#x27;</span><br><span class="line">        &#125;)</span><br><span class="line">        gitalk.render(&#x27;gitalk-container&#x27;)</span><br><span class="line">       &lt;/script&gt;</span><br><span class="line">&#123;% endif %&#125;</span><br></pre></td></tr></table></figure>

<p><em>这里需要说明一下，这里引入了md5.min.js文件，并对博客的pathname进行MD5处理，原因是由于github限制，不能创建超过50字符长度的issue？？？反正就是长度太长就是创建不了。</em></p>
<p><a href="https://github.com/blueimp/JavaScript-MD5/tree/master/js">md5.min.js文件可以在github上下载</a>，下载后放到<code>next\source\js\src</code>目录下即可。</p>
<p>在next主题的<code>\layout\_third-party\comments</code>目录中，更新index.swig文件，在文件末尾增加一行。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">&#123;% include &#x27;gitalk.swig&#x27; %&#125;</span><br></pre></td></tr></table></figure>

<p>在next主题的<code>\layout\_partials</code>目录中，更新comments.swig文件，如下增加gitalk部分即可。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;% elseif theme.<span class="property">valine</span>.<span class="property">appid</span> and theme.<span class="property">valine</span>.<span class="property">appkey</span> %&#125;</span><br><span class="line">  &lt;div <span class="keyword">class</span>=<span class="string">&quot;comments&quot;</span> id=<span class="string">&quot;comments&quot;</span>&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line"></span><br><span class="line">&#123;% elseif theme.<span class="property">gitalk</span>.<span class="property">enable</span> %&#125;</span><br><span class="line">  &lt;div id=<span class="string">&quot;gitalk-container&quot;</span>&gt;&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<p>待所有内容修改完毕之后，保存并执行如下命令，进行发布。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure>

<p>然后登陆github pages网页查看效果。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/image-20220714113512240.png" alt="image-20220714113512240"></p>
]]></content>
      <categories>
        <category>差生文具多</category>
      </categories>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>neovim配置方案</title>
    <url>/2023/08/23/neovim%E9%85%8D%E7%BD%AE%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<h3 id="什么是neovim"><a href="#什么是neovim" class="headerlink" title="什么是neovim"></a>什么是neovim</h3><p>Neovim 是一个基于 Vim 项目代码库开发的新项目，它与 Vim 保持了高度兼容性（尤其是在配置和大多数插件方面），并且提供了许多 Vim 所不具备的、面向现代开发需求的优秀功能，例如异步 I&#x2F;O、内置终端和原生的 Lua 脚本支持等。</p>
<span id="more"></span>

<h3 id="安装neovim"><a href="#安装neovim" class="headerlink" title="安装neovim"></a>安装neovim</h3><p>以windos为例，直接从github上下载对应版本即可。<br>下载地址是这里：<a href="https://github.com/neovim/neovim/releases">Neovim下载地址</a>, 我这里选择的版本是:<code>v0.9.1</code>。</p>
<p>将下载好的neovim zip包解压至指定目录，并将<code>&#123;NEOVIM_HOME&#125;\bin</code>目录配置到系统环境变量中，即可完成对neovim的安装。</p>
<p>针对windows版本，neovim提供了两个命令：<code>nvim.exe</code>和<code>nvim-qt.exe</code>,前者是命令行式的，后者提供一个图形化界面。这里常用的式<code>nvim.exe</code>，也可以简写为<code>nvim</code></p>
<p>我们可以通过如下命令验证neovim及其环境变量是否安装成功。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">打开powershell，输入如下命令</span></span><br><span class="line">nvim --verion </span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">会打印如下结果</span></span><br><span class="line">NVIM v0.9.1</span><br><span class="line">Build type: RelWithDebInfo</span><br><span class="line">LuaJIT 2.1.0-beta3</span><br><span class="line">Compilation: C:/Program Files (x86)/Microsoft Visual Studio/2019/Enterprise/VC/Tools/MSVC/14.29.30133/bin/Hostx64/x64/cl.exe /MD /Zi /O2 /Ob1  -W3 -wd4311 -wd4146 -DUNIT_TESTING -D_CRT_SECURE_NO_WARNINGS -D_CRT_NONSTDC_NO_DEPRECATE -D_WIN32_WINNT=0x0602 -DMSWIN -DINCLUDE_GENERATED_DECLARATIONS -ID:/a/neovim/neovim/.deps/usr/include/luajit-2.1 -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/build/src/nvim/auto -ID:/a/neovim/neovim/build/include -ID:/a/neovim/neovim/build/cmake.config -ID:/a/neovim/neovim/src -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include -ID:/a/neovim/neovim/.deps/usr/include</span><br><span class="line"></span><br><span class="line">   system vimrc file: &quot;$VIM\sysinit.vim&quot;</span><br><span class="line">  fall-back for $VIM: &quot;C:/Program Files (x86)/nvim/share/nvim&quot;</span><br><span class="line"></span><br><span class="line">Run :checkhealth for more info</span><br></pre></td></tr></table></figure>

<p><strong>neovim自检</strong></p>
<p>在powershell中输入nvim打开neovim，在normal模式下，输入命令<code>:checkhealth</code>，可以实现对neovim的自检工作。  </p>
<p>同时，如果没有配置neovim所需要的配置文件的话，自检结果还会显示需要我们在指定路径下新建neovim所需要的配置文件。 </p>
<p>对于windows而言，需要在<code>$&#123;user&#125;\AppData\Local\nvim</code>目录下新建<code>init.lua</code>文件，如果文件夹不存在，需要手动创建。</p>
<h3 id="neovim-配置"><a href="#neovim-配置" class="headerlink" title="neovim 配置"></a>neovim 配置</h3><h4 id="packer-vim"><a href="#packer-vim" class="headerlink" title="packer.vim"></a>packer.vim</h4><p>packer.nvim是neovim的插件管理工具，基于该工具可以安装我们所需的插件。</p>
<p>要想使用packer.vim，需要首先从github上下载该安装包，并放到指定路径下，对于windows用户而言,默认需要将其放到这个目录中：<code>$&#123;user&#125;\AppData\Local\nvim-data\site\pack\packer\start</code>,如果该目录不存在，请手动创建。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cd $&#123;user&#125;\AppData\Local\nvim-data\site\pack\packer\start</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">下载packer.nvim</span></span><br><span class="line">git clone https://github.com/wbthomason/packer.nvim</span><br></pre></td></tr></table></figure>

<p>然后进入到neovim的配置目录（<code>$&#123;user&#125;\AppData\Local\nvim</code>），新建一个文件夹，文件夹名字随意，这里我选择名字为<code>lua</code>,然后进入到lua文件夹中，新建一个lua文件，名字随意，这里我选择新建一个<code>plugin_manager.lua</code>.然后在文件中写入如下内容。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> ensure_packer = <span class="function"><span class="keyword">function</span><span class="params">()</span></span></span><br><span class="line">  <span class="keyword">local</span> fn = vim.fn</span><br><span class="line">  <span class="keyword">local</span> install_path = fn.stdpath(<span class="string">&#x27;data&#x27;</span>)..<span class="string">&#x27;/site/pack/packer/start/packer.nvim&#x27;</span></span><br><span class="line">  <span class="keyword">if</span> fn.empty(fn.glob(install_path)) &gt; <span class="number">0</span> <span class="keyword">then</span></span><br><span class="line">    fn.system(&#123;<span class="string">&#x27;git&#x27;</span>, <span class="string">&#x27;clone&#x27;</span>, <span class="string">&#x27;--depth&#x27;</span>, <span class="string">&#x27;1&#x27;</span>, <span class="string">&#x27;https://github.com/wbthomason/packer.nvim&#x27;</span>, install_path&#125;)</span><br><span class="line">    vim.cmd <span class="string">[[packadd packer.nvim]]</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> packer_bootstrap = ensure_packer()</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="built_in">require</span>(<span class="string">&#x27;packer&#x27;</span>).startup(<span class="function"><span class="keyword">function</span><span class="params">(use)</span></span></span><br><span class="line">  use <span class="string">&#x27;wbthomason/packer.nvim&#x27;</span></span><br><span class="line">  <span class="comment">-- My plugins here</span></span><br><span class="line">  <span class="comment">-- use &#x27;foo1/bar1.nvim&#x27;</span></span><br><span class="line">  <span class="comment">-- use &#x27;foo2/bar2.nvim&#x27;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">-- Automatically set up your configuration after cloning packer.nvim</span></span><br><span class="line">  <span class="comment">-- Put this at the end after all plugins</span></span><br><span class="line">  <span class="keyword">if</span> packer_bootstrap <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">require</span>(<span class="string">&#x27;packer&#x27;</span>).sync()</span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span>)</span><br></pre></td></tr></table></figure>
<p>最后，再在<code>$&#123;user&#125;\AppData\Local\nvim\init.lua</code>中添加如下内容，表示使用该插件。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>至此，packer.nvim插件管理工具安装过完成。</p>
<p>可以通过打开neovim，并在normal模式下执行<code>:PackerSync</code>来检查我们的安装效果。</p>
<h4 id="文档树插件nvim-tree"><a href="#文档树插件nvim-tree" class="headerlink" title="文档树插件nvim-tree"></a>文档树插件<code>nvim-tree</code></h4><p>nvim-tree能够使得neovim实现如下形式的功能。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822213140.png" alt="20230822213140"><br>安装该插件的方式非常简单，在<code>plugin_manager.lua</code>文件中的<code>-- use &#39;foo2/bar2.nvimm</code>和<code>  -- Automatically set up your configuration after cloning packer.nvim</code>之间添加如下内容即可：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">use &#123;</span><br><span class="line">  <span class="string">&#x27;nvim-tree/nvim-tree.lua&#x27;</span>,</span><br><span class="line">  requires = &#123;</span><br><span class="line">    <span class="string">&#x27;nvim-tree/nvim-web-devicons&#x27;</span>, <span class="comment">-- optional</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后保存，重启neovim，在normal模式下输入命令<code>:PackerSync</code>即可实现对该插件的安装。</p>
<p>为了更好的使用<code>nvim-tree</code>，需要对该插件进行额外的配置。<br>在<code>lua</code>文件夹下创建一个<code>plugins-config</code>文件夹，并在该文件夹中创建<code>nvim-tree</code>的配置文件<code>nvim-tree.lua</code>,内容如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 表示默认情况下不打开nvim-tree</span></span><br><span class="line">vim.g.loaded_netrw = <span class="number">1</span></span><br><span class="line">vim.g.loaded_netrwPlugin = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&quot;nvim-tree&quot;</span>).setup()</span><br></pre></td></tr></table></figure>

<p>然后在<code>init.lua</code>配置文件中，指定对该配置的使用：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 指定lua目录下的plugins-config/vim-tree.lua配置文件，lua后缀默认可缺省</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.nvim-tree&#x27;</span>)</span><br></pre></td></tr></table></figure>

<p>保存并重启neovim，在normal模式下使用命令<code>:NvimTreeToggle</code>,就可以使用文档树的功能了。</p>
<p><code>:NvimTreeToggle</code>命令太长了，输入有些麻烦，neovim支持快捷键，因此我们可以设置快捷键来实现该功能。<br>在<code>lua</code>文件夹下创建一个core文件夹，并在该文件夹下创建keymaps.lua文件，专门用于设置各种快捷键。为实现上述快捷键功能，填写如下内容。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 设置&lt;leader&gt;表示一个空格键</span></span><br><span class="line">vim.g.mapleader = <span class="string">&quot; &quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 提供公共部分</span></span><br><span class="line"><span class="keyword">local</span> keymap = vim.keymap</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 设定:NvimTreeToggle的快捷键是空格 + e</span></span><br><span class="line">keymap.set(<span class="string">&quot;n&quot;</span>, <span class="string">&quot;&lt;leader&gt;e&quot;</span>, <span class="string">&quot;:NvimTreeToggle&lt;CR&gt;&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>于此同时，在<code>init.lua</code>中引用该快捷键配置。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 指定lua目录下的plugins-config/vim-tree.lua配置文件，lua后缀默认可缺省</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.nvim-tree&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 引用快捷键</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.keymaps&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>保存重启neovim，就可以按快捷键<code>空格 + e</code>键，快速打开文档树。</p>
<p><code>nvim-tree</code>首先会有文档树的导航功能，其次它还会对不同的文件按照不同图标形式显示。为了实现图标的显示，需要安装<code>nerd font</code>字体。</p>
<h4 id="nerd-font字体安装"><a href="#nerd-font字体安装" class="headerlink" title="nerd font字体安装"></a><code>nerd font</code>字体安装</h4><p>所谓Nerd Fonts 是一个使用大量字体图标来解决程序员在开发过程中缺少合适字体的问题的项目。<br>举个例子诸如如下的图标，我们在<code>powershell</code>中是不能直接粘贴复制的。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822213314.png" alt="20230822213314"></p>
<p>但是一旦当前字体支持<code>nerd font</code>,就会实现快速的粘贴，如下所示。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822213524.png" alt="20230822213524"></p>
<p>可以在这个网站上选择自己喜欢的<code>nerd font</code>字体：<code>https://www.nerdfonts.com/font-downloads</code>，下载字体包，解压并安装。<br>我这里选择的是:<code>font-sourcecodepro-nerd-fon</code>，字体效果如以下所示。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822214558.png" alt="20230822214558"></p>
<p>字体安装之后，重启<code>powershell</code>,然后再<code>powershell</code>的<code>设置 - 默认值 - 外观 - 字体</code>中选择目标字体，即可完成设置，再次打开neovim即可实现上面nvim-tree中的文档树效果及图标显示。</p>
<p>该字体不仅仅再这里需要，后面很多需要图标的插件都需要这种类型的字体。</p>
<h4 id="主题插件"><a href="#主题插件" class="headerlink" title="主题插件"></a>主题插件</h4><p>tokyonight是一个主题插件，支持4种主题：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">tokyonight-night</span><br><span class="line">tokyonight-storm</span><br><span class="line">tokyonight-day</span><br><span class="line">tokyonight-moon</span><br></pre></td></tr></table></figure>
<p>安装方式和安装<code>nvim-tree</code>方式一样，也是现在<code>plugins_manager.lua</code>中声明，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">use <span class="string">&#x27;folke/tokyonight.nvim&#x27;</span></span><br></pre></td></tr></table></figure>
<p>要使该主题生效，需要进行声明，为方便管理，我再<code>lua</code>文件夹下创建一个core文件夹，然后新建文件<code>options.lua</code>文件，填写如下内容：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 使用tokyonight主题</span></span><br><span class="line">vim.cmd<span class="string">[[colorscheme tokyonight]]</span></span><br></pre></td></tr></table></figure>

<p>同时在<code>init.lua</code>文件中声明：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 指定lua目录下的plugins-config/vim-tree.lua配置文件，lua后缀默认可缺省</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.nvim-tree&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 引用快捷键</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.keymaps&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用options</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.options&#x27;</span>)</span><br></pre></td></tr></table></figure>

<h4 id="标签插件"><a href="#标签插件" class="headerlink" title="标签插件"></a>标签插件</h4><p>neovim默认的标签也不好，可以使用bufferline插件，实现一个好看的标签页，如下图所示。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822225027.png" alt="20230822225027"></p>
<p>安装该插件的方式和安装<code>nvim-tree</code>一样，也是现在<code>plugins_manager.lua</code>中声明，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">use &#123;<span class="string">&#x27;akinsho/bufferline.nvim&#x27;</span>, tag = <span class="string">&quot;*&quot;</span>, requires = <span class="string">&#x27;nvim-tree/nvim-web-devicons&#x27;</span>&#125;</span><br></pre></td></tr></table></figure>
<p>该插件需要进行一些配置，以便于更好的使用。在plugins-config目录下新建<code>bufferline.lua</code>,填写如下内容：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">vim.opt.termguicolors = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&quot;bufferline&quot;</span>).setup &#123;</span><br><span class="line">    options = &#123;</span><br><span class="line">        <span class="comment">-- 使用 nvim 内置lsp</span></span><br><span class="line">        diagnostics = <span class="string">&quot;nvim_lsp&quot;</span>,</span><br><span class="line">        <span class="comment">-- 左侧让出 nvim-tree 的位置</span></span><br><span class="line">        offsets = &#123;&#123;</span><br><span class="line">            filetype = <span class="string">&quot;NvimTree&quot;</span>,</span><br><span class="line">            text = <span class="string">&quot;File Explorer&quot;</span>,</span><br><span class="line">            highlight = <span class="string">&quot;Directory&quot;</span>,</span><br><span class="line">            text_align = <span class="string">&quot;left&quot;</span></span><br><span class="line">        &#125;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同时在<code>init.lua</code>声明对该配置的使用。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 指定lua目录下的plugins-config/vim-tree.lua配置文件，lua后缀默认可缺省</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.nvim-tree&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用bufferline插件配置</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.bufferline&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 引用快捷键</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.keymaps&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用options</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.options&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>保存重启neovim，执行<code>:PackerSync</code>命令实现对插件的安装，再次重启neovim即可使用该插件功能。</p>
<h4 id="状态栏插件安装"><a href="#状态栏插件安装" class="headerlink" title="状态栏插件安装"></a>状态栏插件安装</h4><p>这里需要的插件是<code>lualine</code>,该插件实现的功能是neovim的地步状态栏更加好看。如下图所示。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822222242.png" alt="20230822222242"></p>
<p>安装该插件的方式和安装<code>nvim-tree</code>一样，也是现在<code>plugins_manager.lua</code>中声明，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">use &#123;</span><br><span class="line">   <span class="string">&#x27;nvim-lualine/lualine.nvim&#x27;</span>,  <span class="comment">-- 状态栏</span></span><br><span class="line">   requires = &#123; <span class="string">&#x27;kyazdani42/nvim-web-devicons&#x27;</span>, opt = <span class="literal">true</span> &#125;  <span class="comment">-- 状态栏图标</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同样重启neovim，执行<code>:PackerSync</code>完成对该插件的安装工作。</p>
<p>该插件同样需要一些配置，因此在<code>plugins-config</code>目录下新建<code>lualine.lua</code>配置文件，并填写如下内容：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;lualine&#x27;</span>).setup(&#123;</span><br><span class="line">  options = &#123;</span><br><span class="line">    theme = <span class="string">&#x27;tokyonight&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>然后在<code>init.lua</code>中引用该配置。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 指定lua目录下的plugins-config/vim-tree.lua配置文件，lua后缀默认可缺省</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.nvim-tree&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用bufferline插件配置</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.bufferline&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用lualine插件配置</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.lualine&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 引用快捷键</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.keymaps&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用options</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.options&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>然后重启neovim，就可以发现状态变成上图所示的形式了。</p>
<h4 id="全文检索插件"><a href="#全文检索插件" class="headerlink" title="全文检索插件"></a>全文检索插件</h4><p>这里需要的插件是<code>telescope</code>,该插件实现的功能是能够在项目中对文件及文件内容进行检索。如下图所示。</p>
<p><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822230657.png" alt="20230822230657"></p>
<p>安装该插件的方式和安装<code>nvim-tree</code>一样，也是现在<code>plugins_manager.lua</code>中声明，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">use &#123;</span><br><span class="line">   <span class="string">&#x27;nvim-telescope/telescope.nvim&#x27;</span>, tag = <span class="string">&#x27;0.1.1&#x27;</span>,  <span class="comment">-- 文件检索</span></span><br><span class="line">   requires = &#123; &#123;<span class="string">&#x27;nvim-lua/plenary.nvim&#x27;</span>&#125; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>同样重启neovim，执行<code>:PackerSync</code>完成对该插件的安装工作。</p>
<p>该插件同样需要一些配置，因此在<code>plugins-config</code>目录下新建<code>telescope.lua</code>配置文件，并填写如下内容：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> builtin = <span class="built_in">require</span>(<span class="string">&#x27;telescope.builtin&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 进入telescope页面会是插入模式，回到正常模式就可以用j和k来移动了</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 搜索文件名</span></span><br><span class="line">vim.keymap.set(<span class="string">&#x27;n&#x27;</span>, <span class="string">&#x27;&lt;leader&gt;ff&#x27;</span>, builtin.find_files, &#123;&#125;)</span><br><span class="line"><span class="comment">-- 搜索文件内容</span></span><br><span class="line">vim.keymap.set(<span class="string">&#x27;n&#x27;</span>, <span class="string">&#x27;&lt;leader&gt;fg&#x27;</span>, builtin.live_grep, &#123;&#125;)  <span class="comment">-- 环境里要安装ripgrep</span></span><br><span class="line">vim.keymap.set(<span class="string">&#x27;n&#x27;</span>, <span class="string">&#x27;&lt;leader&gt;fb&#x27;</span>, builtin.buffers, &#123;&#125;)</span><br><span class="line">vim.keymap.set(<span class="string">&#x27;n&#x27;</span>, <span class="string">&#x27;&lt;leader&gt;fh&#x27;</span>, builtin.help_tags, &#123;&#125;)</span><br></pre></td></tr></table></figure>
<p>然后再<code>init.lua</code>中引用该配置。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 其中lua文件夹这个路径可以缺省，neovim会自动在该目录下的lua文件夹中寻找</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins_manager&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 指定lua目录下的plugins-config/vim-tree.lua配置文件，lua后缀默认可缺省</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.nvim-tree&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用bufferline插件配置</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.bufferline&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用lualine插件配置</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.lualine&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用telescope插件配置</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;plugins-config.telescop&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 引用options</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.options&#x27;</span>)</span><br><span class="line"><span class="comment">-- 引用options</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">&#x27;core.keymaps&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>然后重启neovim，按快捷键<code>空格 + f + f</code>就可以实现对文件的搜索了。</p>
<p><strong>安装ripgrep</strong>  </p>
<p>要实现对文件内容的检索，需要安装<code>ripgrep</code>.<br>首先下载该安装包，可以在这里下载：<code>https://github.com/BurntSushi/ripgrep/releases</code>. 解压，安装，并将其添加至环境变量中，即完成了该工具包的安装。<br>然后重启<code>powershell</code>,重新打开neovim，按<code>空格 + f + g</code>即可实现对文件内容的搜索，如下图所示。<br><img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/20230822233116.png" alt="20230822233116"></p>
<h4 id="vim-tmux-navigator插件"><a href="#vim-tmux-navigator插件" class="headerlink" title="vim-tmux-navigator插件"></a>vim-tmux-navigator插件</h4><p>该插件可以实现焦点快速从文件侧转移至文档树导航侧，或者反过来。</p>
<p>安装该插件的方式和安装<code>nvim-tree</code>一样，也是现在<code>plugins_manager.lua</code>中声明，如下所示：</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">use <span class="string">&quot;christoomey/vim-tmux-navigator&quot;</span></span><br></pre></td></tr></table></figure>
<p>同样重启neovim，执行<code>:PackerSync</code>完成对该插件的安装工作。</p>
<p>该插件安装成功之后，重启neovim，按<code>ctrl + H</code>,<code>ctrl + L</code> 快速实现焦点切换。</p>
<h4 id="其他的一些配置"><a href="#其他的一些配置" class="headerlink" title="其他的一些配置"></a>其他的一些配置</h4><p>在options.lua文件中配置。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> opt = vim.opt</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 显示行号</span></span><br><span class="line">opt.number = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- vim中的粘贴板和系统中的粘贴板共通</span></span><br><span class="line">opt.clipboard = <span class="string">&quot;unnamedplus&quot;</span></span><br><span class="line"></span><br><span class="line">opt.termguicolors = <span class="literal">true</span></span><br><span class="line">opt.signcolumn = <span class="string">&quot;yes&quot;</span></span><br><span class="line">vim.cmd<span class="string">[[colorscheme tokyonight-moon]]</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>差生文具多</category>
      </categories>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>思念到极致的感觉--知乎上有趣的回答</title>
    <url>/2024/06/13/%E6%80%9D%E5%BF%B5%E5%88%B0%E6%9E%81%E8%87%B4%E7%9A%84%E6%84%9F%E8%A7%89-%E7%9F%A5%E4%B9%8E%E4%B8%8A%E6%9C%89%E8%B6%A3%E7%9A%84%E5%9B%9E%E7%AD%94/</url>
    <content><![CDATA[<p>原文：思念到极致是什么感觉？ - 大头大头下雨不愁的回答 - 知乎<br><a href="https://www.zhihu.com/question/31914864/answer/3421297405">https://www.zhihu.com/question/31914864/answer/3421297405</a></p>
<img src="https://cdn.jsdelivr.net/gh/guluo2016/picture@dev/img/huduisongdesinian20240613.jpg" alt="image description" width="350"/>

]]></content>
      <categories>
        <category>有趣</category>
      </categories>
  </entry>
  <entry>
    <title>行式/列式/列族式数据库</title>
    <url>/2023/05/30/%E8%A1%8C%E5%BC%8F-%E5%88%97%E5%BC%8F-%E5%88%97%E6%97%8F%E5%BC%8F%E6%95%B0%E6%8D%AE%E5%BA%93/</url>
    <content><![CDATA[<p>TODO</p>
]]></content>
      <categories>
        <category>基础</category>
      </categories>
  </entry>
</search>
